services:
  influxdb:
    image: influxdb:2
    container_name: influxdb
    restart: unless-stopped
    mem_limit: 512m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    environment:
      DOCKER_INFLUXDB_INIT_MODE: setup
      DOCKER_INFLUXDB_INIT_USERNAME: ${INFLUX_USER}
      DOCKER_INFLUXDB_INIT_PASSWORD: ${INFLUX_PASS}
      DOCKER_INFLUXDB_INIT_ORG: homebrew
      DOCKER_INFLUXDB_INIT_BUCKET: fermentation
      DOCKER_INFLUXDB_INIT_ADMIN_TOKEN: ${INFLUX_TOKEN}
    ports:
      - "8086:8086"
    volumes:
      - ./influxdb_data:/var/lib/influxdb2
    networks:
      - brewery-net

  telegraf:
    image: telegraf:latest
    container_name: telegraf
    restart: unless-stopped
    mem_limit: 128m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    environment:
      - INFLUX_TOKEN=${INFLUX_TOKEN}
      - INFLUX_ORG=homebrew
      - INFLUX_BUCKET=fermentation
      - INFLUX_URL=http://influxdb:8086
    volumes:
      - ./telegraf.conf:/etc/telegraf/telegraf.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - brewery-net

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    restart: unless-stopped
    mem_limit: 256m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ALLOW_EMBEDDING=true
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
    volumes:
      - ./grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
    networks:
      - brewery-net
    depends_on:
      - influxdb

  brew-brain:
    build: ./app
    container_name: brew-brain
    working_dir: /app
    restart: unless-stopped
    mem_limit: 384m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    ports:
      - "5000:5000"
    environment:
      - INFLUX_URL=http://influxdb:8086
      - INFLUX_TOKEN=${INFLUX_TOKEN}
      - INFLUX_ORG=homebrew
      - INFLUX_BUCKET=fermentation
      - TZ=Europe/London
      - BREW_BRAIN_DATA=/data
      - BREW_BRAIN_API_TOKEN=${BREW_BRAIN_API_TOKEN:-secure_default_token}
      - REDIS_URL=redis://redis:6379/0
      - OLLAMA_HOST=ollama
      - PYTHONPATH=/app
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:5000/api/status" ]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s
    volumes:
      - ./brain_data:/data
    networks:
      - brewery-net
    depends_on:
      - influxdb
      - redis

  web:
    build: ./web
    container_name: brew-brain-web
    restart: unless-stopped
    mem_limit: 512m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - API_HOST=brew-brain
      - BREW_BRAIN_API_TOKEN=${BREW_BRAIN_API_TOKEN:-secure_default_token}
    networks:
      - brewery-net
    depends_on:
      - brew-brain

  celery-worker:
    build: ./app
    container_name: celery-worker
    command: celery -A extensions.celery worker --loglevel=info
    working_dir: /app
    restart: unless-stopped
    mem_limit: 512m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    environment:
      - INFLUX_URL=http://influxdb:8086
      - INFLUX_TOKEN=${INFLUX_TOKEN}
      - INFLUX_ORG=homebrew
      - INFLUX_BUCKET=fermentation
      - REDIS_URL=redis://redis:6379/0
      - BREW_BRAIN_DATA=/data
      - PYTHONPATH=/app
      - TZ=Europe/London
    volumes:
      - ./brain_data:/data
    networks:
      - brewery-net
    depends_on:
      - redis
      - influxdb

  celery-beat:
    build: ./app
    container_name: celery-beat
    command: celery -A extensions.celery beat --loglevel=info
    working_dir: /app
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    environment:
      - INFLUX_URL=http://influxdb:8086
      - INFLUX_TOKEN=${INFLUX_TOKEN}
      - INFLUX_ORG=homebrew
      - INFLUX_BUCKET=fermentation
      - REDIS_URL=redis://redis:6379/0
      - BREW_BRAIN_DATA=/data
      - PYTHONPATH=/app
      - TZ=Europe/London
    volumes:
      - ./brain_data:/data
    networks:
      - brewery-net
    depends_on:
      - redis
      - influxdb

  redis:
    image: redis:alpine
    container_name: redis
    command: redis-server --maxmemory 128mb --maxmemory-policy allkeys-lru
    restart: unless-stopped
    # Port intentionally NOT exposed to host — Redis accessible only within brewery-net.
    # To connect locally for debugging: docker exec -it redis redis-cli
    networks:
      - brewery-net

  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    restart: unless-stopped
    volumes:
      - ./ollama_data:/root/.ollama
    ports:
      - "11434:11434"
    networks:
      - brewery-net


networks:
  brewery-net:
    driver: bridge

